/** Injected tip marker so Expo Go / TestFlight can prove which commit is running. */
export const BUILD_INFO = {
  branch: 'cursor/make-v18',
  commit: '29d3060',
  label: 'make-v18 · live-sync · session-presence · task-reminders',
} as const;
