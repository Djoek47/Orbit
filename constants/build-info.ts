/** Injected tip marker so Expo Go / TestFlight can prove which commit is running. */
export const BUILD_INFO = {
  branch: 'cursor/admin-member-connection-c30d',
  commit: 'c391757',
  label: 'admin-profiles · connection-badges',
} as const;
