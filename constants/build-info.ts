/** Injected tip marker so Expo Go / TestFlight can prove which commit is running. */
export const BUILD_INFO = {
  branch: 'cursor/auto-approval-c30d',
  commit: 'pending',
  label: 'connection-flow · join-policy · supabase-only',
} as const;
