/** Injected tip marker so Expo Go / TestFlight can prove which commit is running. */
export const BUILD_INFO = {
  branch: 'cursor/member-connection-v16',
  commit: '9493ee7',
  label: 'member-v16 · connection-flow',
} as const;
