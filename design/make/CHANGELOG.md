# Make sync changelog

## 2026-07-16 — Make v5 + Family Time OS polish

- Synced via Figma MCP `get_design_context` (`fileKey=4J6d4LW335tDyEDpqq3VD1`, `nodeId=0:1`).
- **Tabs (v5):** Home · Tasks · **Plan** (Calendar | Itineraries sub-nav) · Ranks · Nova.
- Grocery intelligence screen remains in Make source but is **not** a primary tab; Expo keeps `/(tabs)/groceries` as a deep-link / Settings / Plan entry.
- Tokens aligned to Make `theme.css`: `#070D1C` bg, `#EEF2FF` text, `#7C9CC0` muted, `#38BDF8` primary, `#06B6D4` Nova accent, `#A78BFA` Plan.
- Itinerary Smart Trips UI + progressive Maps handoff ported under Plan / `itinerary/[id]`.
- Snapshot notes under `design/make/source/v5/`.

## Earlier — Family Time OS polish (pre-auth)

- Figma MCP was `needsAuth`; provisional tokens from UX docs until v5 sync.
