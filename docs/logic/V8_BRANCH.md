# Choremaxx Make v8 shipping branch

**Git branch:** `cursor/choremaxx-make-v8-5f8f`  
**Forked from:** `cursor/choremaxx-make-v7-c30d` @ `ae15027` (Day/Night fonts + Meritocracy/Equity Logic foundations)

## Purpose

v8 is the next shipping line after Make v7. Design Make is already marked `makeVersion: "v8"` in [`design/make/SYNC_STATE.json`](../design/make/SYNC_STATE.json) (Design 8 glass + Nova v9).

## Carry-over from v7 tip

- Day/Night live palette text tokens
- Reward mode (Meritocracy / Equity) + hygiene guard + `StreakMarker`
- Streak engine foundations + trophy evaluator architecture
- Specs under `docs/logic/`

## Expo Go

EAS Update branch for this line: `cursor/choremaxx-make-v8-5f8f` (publish with `eas update --branch cursor/choremaxx-make-v8-5f8f`).
